token: {{ rke2_token }}
server: https://{{ rke2_server_url }}:9345
node-label:
  - "node-role=worker"
{% for key, value in rke2_node_labels | default({}) | items %}
  - "{{ key }}={{ value }}"
{% endfor %}
